# secrets — never commit. `.env*` catches .env, .env2, .env.local, .env.bak;
# `*.env` catches prod.env. Templates are re-allowed explicitly.
.env*
*.env
!.env.example

# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# local storage / IDE / OS
storage/
.idea/
.DS_Store
.coverage
